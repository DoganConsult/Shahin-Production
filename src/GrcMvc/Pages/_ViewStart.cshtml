@*
    Pages _ViewStart - Overrides ABP's default _ViewStart
    Our custom Account pages use Layout = null so they don't need a layout.
    This file prevents ABP's theming system from being invoked.
*@
@{
    // Don't set a default layout - let each page decide
    // This prevents ABP's theme manager from being called
}
