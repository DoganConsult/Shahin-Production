@model string

@if (!string.IsNullOrEmpty(Model))
{
    <a href="mailto:@Model" class="grc-email text-decoration-none">
        <i class="bi bi-envelope me-1"></i>
        @Model
    </a>
}
else
{
    <span class="text-muted">—</span>
}
