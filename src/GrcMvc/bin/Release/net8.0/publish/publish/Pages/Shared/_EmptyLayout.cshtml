@*
    Empty Layout - Used by ABP Theme to satisfy theming requirements.
    Our custom Account pages (Login, Register, Logout) use Layout = null
    so they don't actually use this layout.
*@
@RenderBody()
